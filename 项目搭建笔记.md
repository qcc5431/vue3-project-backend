# Vue3 后端项目搭建笔记

## 1. 安装Express、MySQL和其他必要依赖

### 核心依赖
```bash
npm install express mysql2 dotenv
```

### 中间件与工具依赖
```bash
npm install cors body-parser jsonwebtoken bcrypt multer sharp cos-nodejs-sdk-v5
```

### 开发依赖
```bash
npm install --save-dev nodemon
```

### 依赖说明
- **express**: Web应用框架（v5.2.1）
- **mysql2**: MySQL数据库驱动，支持Promise
- **dotenv**: 环境变量管理
- **cors**: 跨域资源共享中间件
- **body-parser**: 请求体解析中间件
- **jsonwebtoken**: JWT认证token生成与验证
- **bcrypt**: 密码加密
- **multer**: 文件上传处理
- **sharp**: 图片处理（获取图片尺寸、压缩等）
- **cos-nodejs-sdk-v5**: 腾讯云对象存储SDK
- **nodemon**: 开发环境自动重启

---

## 2. 创建项目目录结构（src, config, routes, controllers, models等）

### 完整目录结构
```
vue3-project-backend/
├── src/
│   ├── config/              # 配置文件
│   │   └── database.js      # 数据库连接配置
│   ├── controllers/         # 控制器层（业务逻辑）
│   │   ├── folderController.js
│   │   ├── noteController.js
│   │   ├── socialController.js
│   │   ├── uploadController.js
│   │   └── userController.js
│   ├── middleware/          # 中间件
│   │   └── auth.js          # JWT认证中间件
│   ├── routes/              # 路由层
│   │   ├── folderRoutes.js
│   │   ├── noteRoutes.js
│   │   ├── socialRoutes.js
│   │   ├── uploadRoutes.js
│   │   └── userRoutes.js
│   ├── scripts/             # 工具脚本
│   │   └── cleanupOrphanImages.js  # 清理孤儿图片
│   ├── app.js               # Express应用主文件
│   └── server.js            # 服务器启动文件
├── .env                     # 环境变量配置（不提交）
├── .env.example             # 环境变量示例
├── .gitignore               # Git忽略配置
├── database.sql             # 数据库初始化脚本
├── database_incremental.sql # 数据库增量更新脚本
├── ecosystem.config.js      # PM2部署配置
├── package.json             # 项目依赖配置
└── 后端接口设计文档.md     # API接口文档
```

### 架构说明
- **MVC模式**: Model-View-Controller分层架构
- **Routes层**: 定义API路由，调用对应控制器
- **Controllers层**: 处理业务逻辑，操作数据库
- **Middleware层**: 认证、日志、错误处理等中间件
- **Config层**: 数据库、环境变量等配置

---

## 3. 创建数据库配置文件模板

### 文件：`src/config/database.js`

```javascript
const mysql = require("mysql2/promise");
require("dotenv").config();

// 当前运行环境
const NODE_ENV = process.env.NODE_ENV || "development";

// 数据库配置
const dbConfig = {
  host: process.env.DB_HOST,
  port: parseInt(process.env.DB_PORT, 10) || 3306,
  user: process.env.DB_USER,
  password: process.env.DB_PASSWORD,
  database: process.env.DB_NAME,
};

// 输出环境信息
console.log(
  `🔧 当前环境: ${NODE_ENV === "production" ? "生产环境" : "测试环境"}`
);
console.log(
  `🔗 数据库连接: ${dbConfig.host}:${dbConfig.port}/${dbConfig.database}`
);

// 创建数据库连接池
const pool = mysql.createPool({
  host: dbConfig.host,
  port: dbConfig.port,
  user: dbConfig.user,
  password: dbConfig.password,
  database: dbConfig.database,
  waitForConnections: true,
  connectionLimit: 10,
  queueLimit: 0,
});

// 测试数据库连接
const testConnection = async () => {
  try {
    const connection = await pool.getConnection();
    console.log("✅ 数据库连接成功");
    connection.release();
  } catch (error) {
    console.error("❌ 数据库连接失败:", error.message);
    process.exit(1);
  }
};

module.exports = { pool, testConnection, dbConfig };
```

### 环境变量：`.env.example`

```env
# 服务器配置
PORT=3000
NODE_ENV=development

# 数据库配置
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=vue3_backend

# JWT配置
JWT_SECRET=your_jwt_secret_key
JWT_EXPIRES_IN=7d

# 腾讯云COS配置
COS_SECRET_ID=your_cos_secret_id
COS_SECRET_KEY=your_cos_secret_key
COS_BUCKET=web-front-1360774249
COS_REGION=ap-beijing
```

### 使用说明
1. 复制 `.env.example` 为 `.env`
2. 填写实际的数据库和服务配置
3. **重要**：`.env` 已加入 `.gitignore`，不会提交到Git

---

## 4. 创建Express应用主文件（app.js）

### 文件：`src/app.js`

```javascript
const express = require("express");
const cors = require("cors");
const bodyParser = require("body-parser");

// 创建Express应用实例
const app = express();

// 中间件配置
app.use(cors()); // 允许跨域
app.use(bodyParser.json({ limit: "50mb" })); // 解析JSON请求体，支持大文件
app.use(bodyParser.urlencoded({ extended: true, limit: "50mb" })); // 解析URL编码的请求体

// 请求日志中间件
app.use((req, res, next) => {
  console.log(`${new Date().toISOString()} - ${req.method} ${req.url}`);
  next();
});

// 健康检查路由
app.get("/health", (req, res) => {
  res.status(200).json({
    code: 200,
    message: "Server is running",
    data: {
      status: "ok",
      timestamp: new Date().toISOString(),
    },
  });
});

// API路由
const userRoutes = require("./routes/userRoutes");
const noteRoutes = require("./routes/noteRoutes");
const socialRoutes = require("./routes/socialRoutes");
const folderRoutes = require("./routes/folderRoutes");
const uploadRoutes = require("./routes/uploadRoutes");

app.use("/api/users", userRoutes);
app.use("/api/notes", noteRoutes);
app.use("/api/social", socialRoutes);
app.use("/api/folders", folderRoutes);
app.use("/api/upload", uploadRoutes);

// 404处理
app.use((req, res) => {
  res.status(404).json({
    code: 404,
    message: `Cannot ${req.method} ${req.url}`,
  });
});

// 全局错误处理中间件
app.use((err, req, res, next) => {
  console.error("Error:", err.stack);
  res.status(err.status || 500).json({
    code: err.status || 500,
    message: err.message || "Internal Server Error",
    ...(process.env.NODE_ENV === "development" && { stack: err.stack }),
  });
});

module.exports = app;
```

### 功能说明
- **中间件加载**: CORS、Body Parser、日志记录
- **路由注册**: 用户、笔记、社交、文件夹、上传
- **错误处理**: 404和全局错误拦截
- **健康检查**: `/health` 端点用于监控

---

## 5. 创建服务启动文件（server.js）

### 文件：`src/server.js`

```javascript
require("dotenv").config();
const app = require("./app");
const { testConnection } = require("./config/database");

const PORT = process.env.PORT || 3000;

// 启动服务器
const startServer = async () => {
  try {
    // 测试数据库连接
    await testConnection();

    // 启动HTTP服务器
    app.listen(PORT, () => {
      console.log("=================================");
      console.log(`🚀 服务器已启动`);
      console.log(`📍 端口: ${PORT}`);
      console.log(`🌍 环境: ${process.env.NODE_ENV || "development"}`);
      console.log(`🔗 访问地址: http://localhost:${PORT}`);
      console.log(`💚 健康检查: http://localhost:${PORT}/health`);
      console.log("=================================");
    });
  } catch (error) {
    console.error("❌ 服务器启动失败:", error);
    process.exit(1);
  }
};

// 优雅关闭
process.on("SIGTERM", () => {
  console.log("⚠️  收到SIGTERM信号,正在关闭服务器...");
  process.exit(0);
});

process.on("SIGINT", () => {
  console.log("⚠️  收到SIGINT信号,正在关闭服务器...");
  process.exit(0);
});

startServer();
```

### 启动流程
1. 加载环境变量
2. 测试数据库连接
3. 启动HTTP服务
4. 监听优雅关闭信号

---

## 6. 创建示例路由和控制器

### JWT认证中间件：`src/middleware/auth.js`

```javascript
const jwt = require("jsonwebtoken");
require("dotenv").config();

const JWT_SECRET = process.env.JWT_SECRET || "your-secret-key-change-in-production";
const JWT_EXPIRES_IN = process.env.JWT_EXPIRES_IN || "7d";

// 生成 JWT token
const generateToken = (userId, username) => {
  return jwt.sign(
    {
      id: userId,
      username: username,
    },
    JWT_SECRET,
    {
      expiresIn: JWT_EXPIRES_IN,
    }
  );
};

// 验证 JWT token 中间件（必须认证）
const verifyToken = (req, res, next) => {
  try {
    const authHeader = req.headers.authorization;

    if (!authHeader) {
      return res.status(401).json({
        code: 401,
        message: "未提供认证token",
      });
    }

    const token = authHeader.startsWith("Bearer ")
      ? authHeader.slice(7)
      : authHeader;

    if (!token) {
      return res.status(401).json({
        code: 401,
        message: "token格式错误",
      });
    }

    const decoded = jwt.verify(token, JWT_SECRET);

    req.user = {
      id: decoded.id,
      username: decoded.username,
    };

    next();
  } catch (error) {
    if (error.name === "JsonWebTokenError") {
      return res.status(401).json({
        code: 401,
        message: "无效的token",
      });
    }

    if (error.name === "TokenExpiredError") {
      return res.status(401).json({
        code: 401,
        message: "token已过期",
      });
    }

    return res.status(500).json({
      code: 500,
      message: "token验证失败",
      error: error.message,
    });
  }
};

// 可选认证中间件（有token则验证，无token则跳过）
const optionalAuth = (req, res, next) => {
  try {
    const authHeader = req.headers.authorization;

    if (!authHeader) {
      return next();
    }

    const token = authHeader.startsWith("Bearer ")
      ? authHeader.slice(7)
      : authHeader;

    if (!token) {
      return next();
    }

    try {
      const decoded = jwt.verify(token, JWT_SECRET);
      req.user = {
        id: decoded.id,
        username: decoded.username,
      };
    } catch (e) {
      // token无效，但不阻止请求
    }

    next();
  } catch (error) {
    next();
  }
};

module.exports = {
  generateToken,
  verifyToken,
  optionalAuth,
  JWT_SECRET,
};
```

### 路由示例：`src/routes/userRoutes.js`

```javascript
const express = require("express");
const router = express.Router();
const { verifyToken } = require("../middleware/auth");
const userController = require("../controllers/userController");

// 公开路由（无需认证）
router.post("/register", userController.register);
router.post("/login", userController.login);

// 受保护路由（需要认证）
router.get("/profile", verifyToken, userController.getProfile);
router.put("/profile", verifyToken, userController.updateProfile);

module.exports = router;
```

### 控制器示例：`src/controllers/userController.js`

```javascript
const { pool } = require("../config/database");
const bcrypt = require("bcrypt");
const { generateToken } = require("../middleware/auth");

// 用户注册
exports.register = async (req, res) => {
  try {
    const { username, email, password } = req.body;

    // 检查用户是否存在
    const [existingUsers] = await pool.query(
      "SELECT id FROM users WHERE username = ? OR email = ?",
      [username, email]
    );

    if (existingUsers.length > 0) {
      return res.status(400).json({
        code: 400,
        message: "用户名或邮箱已存在",
      });
    }

    // 加密密码
    const hashedPassword = await bcrypt.hash(password, 10);

    // 创建用户
    const [result] = await pool.query(
      "INSERT INTO users (username, email, password) VALUES (?, ?, ?)",
      [username, email, hashedPassword]
    );

    res.status(201).json({
      code: 201,
      message: "注册成功",
      data: {
        userId: result.insertId,
      },
    });
  } catch (error) {
    console.error("注册失败:", error);
    res.status(500).json({
      code: 500,
      message: "注册失败",
      error: error.message,
    });
  }
};

// 用户登录
exports.login = async (req, res) => {
  try {
    const { username, password } = req.body;

    // 查询用户
    const [users] = await pool.query(
      "SELECT id, username, email, password FROM users WHERE username = ?",
      [username]
    );

    if (users.length === 0) {
      return res.status(401).json({
        code: 401,
        message: "用户名或密码错误",
      });
    }

    const user = users[0];

    // 验证密码
    const isPasswordValid = await bcrypt.compare(password, user.password);

    if (!isPasswordValid) {
      return res.status(401).json({
        code: 401,
        message: "用户名或密码错误",
      });
    }

    // 生成token
    const token = generateToken(user.id, user.username);

    res.status(200).json({
      code: 200,
      message: "登录成功",
      data: {
        token,
        user: {
          id: user.id,
          username: user.username,
          email: user.email,
        },
      },
    });
  } catch (error) {
    console.error("登录失败:", error);
    res.status(500).json({
      code: 500,
      message: "登录失败",
      error: error.message,
    });
  }
};
```

---

## 7. 更新package.json的启动脚本

### 完整的 `package.json` scripts配置

```json
{
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "cleanup": "node src/scripts/cleanupOrphanImages.js",
    "cleanup:exec": "node src/scripts/cleanupOrphanImages.js --exec",
    "pm2:start": "pm2 start ecosystem.config.js --env production",
    "pm2:stop": "pm2 stop vue3-backend",
    "pm2:restart": "pm2 restart vue3-backend",
    "pm2:delete": "pm2 delete vue3-backend",
    "pm2:logs": "pm2 logs vue3-backend",
    "pm2:monit": "pm2 monit",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
```

### 脚本说明

| 命令 | 说明 |
|------|------|
| `npm start` | 生产环境启动（直接运行） |
| `npm run dev` | 开发环境启动（热重载） |
| `npm run cleanup` | 检查孤儿图片（预览模式） |
| `npm run cleanup:exec` | 清理孤儿图片（执行删除） |
| `npm run pm2:start` | PM2启动生产环境 |
| `npm run pm2:stop` | PM2停止服务 |
| `npm run pm2:restart` | PM2重启服务 |
| `npm run pm2:logs` | 查看PM2日志 |

### 开发流程

1. **本地开发**
   ```bash
   npm run dev
   ```

2. **生产部署**
   ```bash
   npm run pm2:start
   ```

3. **查看日志**
   ```bash
   npm run pm2:logs
   ```

---

## 数据库设计要点

### 核心表结构
- **users**: 用户表（认证、个人信息）
- **notes**: 笔记表（支持Markdown、封面媒体、图片数组）
- **note_likes**: 点赞记录
- **note_collects**: 收藏记录
- **comments**: 评论表（支持回复）
- **follows**: 关注关系表
- **folders**: 文件夹表
- **folder_notes**: 文件夹-笔记关联表

### 特性
- 使用 `utf8mb4` 字符集（支持表情符号）
- JSON字段存储复杂数据（封面媒体、图片列表）
- 外键级联删除确保数据一致性
- 索引优化查询性能

---

## 项目特色功能

### 1. 图片处理
- 使用 `sharp` 获取图片宽高信息
- 后端直接返回图片尺寸，前端无需计算
- 支持腾讯云COS存储

### 2. JWT认证
- 双模式中间件：`verifyToken`（强制认证）、`optionalAuth`（可选认证）
- Token有效期7天
- 支持Bearer Token格式

### 3. 孤儿图片清理
- 自动检测未被引用的COS图片
- 支持预览和执行两种模式
- 防止存储空间浪费

### 4. 统一响应格式
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {}
}
```

---

## 下一步开发建议

1. ✅ 完成基础CRUD接口
2. ✅ 实现JWT认证体系
3. ✅ 集成腾讯云COS
4. 📝 编写单元测试
5. 📝 添加接口限流
6. 📝 实现日志系统
7. 📝 添加API文档（Swagger）
8. 📝 性能监控与优化

---

*最后更新: 2026-02-28*
